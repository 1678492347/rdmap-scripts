
数据库中：
	all_phenotype_hpo长度为14258，和new_hpo_distance的index长度一样，代表了表型异常下的所有表型（根据hpo网站的原始hpo信息，整理出hpo_relation，然后统计出来的），后来发现，disordertohpo(no0)sorted1中有极少数不属于表型异常的表型，我把这些表型加到了all_phenotype_hpo的末尾（14258+6）。
	all_orpha_hpo长度为5677，和old_new_hpo_distance长度一样，代表了罕见病中的所有表型，是根据项目中用到的disordertohpo(no0)sorted1整理出来的（rgdkb_hpo2disorder中的过期表型已被取代，所以all_orpha_hpo的数量比rgdkb_hpo2disorder中的hpo数量要少，disordertohpo(no0)sorted1中没有过期表型）。
	rgdkb_chpo_old长度为5736，里面包含了绝大多数的all_orpha_hpo，长度和rgdkb_hpo2disorder中的表型数量（SELECT DISTINCT hpo_id FROM rgdkb_hpo2disorder）一样，但是由于rgdkb_hpo2disorder是从orphanet网站整理的，有一些表型在现版本的hpo网站上已经过期了。
	rgdkb_chpo长度为14331，里面包含了所有的all_orpha_hpo和all_phenotype_hpo，也包含了rgdkb_chpo_old，rgdkb_chpo中的信息是多次爬虫得来的，比较全。